spring:
  application:
    name: TPFinalDeliveryMascotero

  datasource:
    url: jdbc:mysql://localhost:3306/delivery_mascotero
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true